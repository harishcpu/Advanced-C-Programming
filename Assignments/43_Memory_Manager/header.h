#include <stdio.h>
#include <stdlib.h>
#include "colors.h"

extern int count, full, ary[], N[][2];
extern int row, N[][2];

extern int AddElement(void*);
extern int DispElement(void*, int);
extern int RemElement(void*);
extern void print_bits(void *);
